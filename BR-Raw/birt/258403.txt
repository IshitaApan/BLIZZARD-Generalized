Multilevel in the same dimension should be prohibited from optional Y-series grouping expression builder

Created attachment 120161 report design Description: Date type multilevel dimension can not be select from optional Y-series grouping dropdown list . It can only be set thru expression builder. Steps to reproduce: 1. Open the attach report and double click the chart. 2. Switch to "select data" tab. 3. Try to choose data["order_date/quarter"] from dropdown list box of optional Y-series grouping. Expected result: The value can be select thru dropdown list box Actual result: The value can not be selected and exception thrown.(see screen snapshot). But data["order_date/quarter"] can be set from expression builder of optional Y-series grouping. And the previous exception was gone after set in expression builder and layout in live preview was correct. ErrorLog: N/A