Duplicate items appears when use group as category.

Created attachment 94680 screenshot description: Duplicate category appears when use group as category. build id: 2.3.0 v20080403-1020 steps to reproduce: 1. New a database with sample db and new a dataset with table custmers.(filter:customernumber<131) 2. Insert a table to layout, binding to the dataset. 3. Insert three groups on the table, grouping on country, state, city respectively. 4. New an aggregation: Function:SUM, datafied:creditlimit, aggregate on group. 5. Insert a chart to layout, sharing the table bindings, select group country as category, select the aggregation as value series. Expected result: One bar for each group with the value of aggregation. Actual result: For each group, there're several bars.See screenshot.