[Jobs] Patch to JobProgressManager

The worked/internalWorked code of JobProgressManager round all work to ints. This is a problem when jobs do a lot of small increments of work. I will attach a patch that fixes this problem. It should be applied to the org.eclipse.ui.workbench project.