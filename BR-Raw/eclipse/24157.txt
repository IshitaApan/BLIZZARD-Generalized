[Dialogs] MessageDialog needs to setFocus on default button

As the MessageDialog by default has no other widgets to set focus on we need to set focus on the default button to prevent it getting slammed by the default SWT behaviour.