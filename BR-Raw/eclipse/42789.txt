[Dialogs] DialogCheck is using "assert" as a variable name

org.eclipse.ui.tests.util.DialogCheck is using "assert" as a variable name, which isn't allowed under the 1.4.x series of JDKs. I believe we are supposed to be moving to 1.4.x compliance.