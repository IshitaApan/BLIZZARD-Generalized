[UI] StackModelFactory does not need 'engine' local

We can remove this from createWidget() 	final IStylingEngine engine = (IStylingEngine) folderContext 			.get(IStylingEngine.SERVICE_NAME); because the engine is now passed in via DI. Replace the refs to "engine" with the local "stylingEngine".