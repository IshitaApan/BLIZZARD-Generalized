transparent png has black background

Created attachment 132252 EActivity.png - from the newsgroup, I've verified it on win2000 and vista - works on all other platforms - could be a dup of bug 165751, but not sure - run the snippet below with the attached image in the same dir - on win32 it appears black, on all other platforms it appears blue public static void main(String[] args) { final Display display = new Display(); final Shell shell = new Shell(display); Image image = new Image(null, CircularImage.class.getResourceAsStream("EActivity.png")); Rectangle rect = image.getBounds(); // Create a 100 x 100 image data with a 2-color (i.e. depth 1) palette Color black = display.getSystemColor(SWT.COLOR_BLACK); Color white = display.getSystemColor(SWT.COLOR_WHITE); PaletteData palette = new PaletteData(new RGB[] { black.getRGB(), white.getRGB(), // pixel 1 = white }); ImageData imageData = new ImageData(rect.width, rect.height, 1, palette); imageData.transparentPixel = 1; Image imageOverlay = new Image(display, imageData); GC gc = new GC(imageOverlay); gc.setBackground(white); gc.fillRectangle(rect.x, rect.y, rect.width, rect.height); gc.setBackground(display.getSystemColor(SWT.COLOR_BLUE)); gc.fillOval(rect.x, rect.y, rect.width, rect.height); gc.drawImage(image, rect.x, rect.y); gc.dispose(); imageData = imageOverlay.getImageData(); imageOverlay.dispose(); final Image icon = new Image(display, imageData, imageData.getTransparencyMask()); // Draw the "circular" image on the shell in its paint event shell.addPaintListener(new PaintListener() { public void paintControl(PaintEvent event) { event.gc.drawImage(icon, 10, 10); } }); shell.setBounds(10, 10, 200, 200); shell.open(); while (!shell.isDisposed()) { if (!display.readAndDispatch()) display.sleep(); } black.dispose(); white.dispose(); icon.dispose(); display.dispose(); }