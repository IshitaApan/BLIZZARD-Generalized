[quick assist] convert to enhanced for loop changes semantics for multiple-assigned variable

HEAD, follow-up to bug 264421 When you inline the variable 'path', you get the snippet below. Converting to enhanced for loop again changes semantics, since 'output' is assigned more than once. public class ForLoopTest2 { public static void main(String[] args) { String[] src = new String[] { "1", "2", "3", "4" }; for (int i = 0; i < src.length; i++) { String output = src[i]; if (output.length() == 1) { output = output + "-XXX"; } System.err.println("path="+ src[i] + ",output="+output); } } }